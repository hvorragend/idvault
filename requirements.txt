# idvault – Python-Abhängigkeiten
# pip install -r requirements.txt

# Web-Framework
flask>=3.0.0

# CSRF-Schutz (VULN-A)
flask-wtf>=1.2.0

# Rate-Limiting am Login (VULN-J)
flask-limiter>=3.5.0

# HTML-Sanitizing für Rich-Text-Felder (VULN-C)
bleach[css]>=6.1.0

# Excel-Export
openpyxl>=3.1.0

# LDAP-Authentifizierung (Active Directory / LDAPS)
ldap3>=2.9.1

# Verschlüsselung für LDAP-Service-Account-Passwort
cryptography>=42.0.0

# Produktion (optional)
gunicorn>=21.0.0

# Optional: Windows-Dateieigentümer auslesen (nur auf Windows)
pywin32>=306

# Optional: Schnelleres Hashing großer Dateien
xxhash>=3.0.0

# Teams-Scanner: Microsoft Graph API (optional, nur für teams_scanner.py benötigt)
msal>=1.28.0
requests>=2.31.0
