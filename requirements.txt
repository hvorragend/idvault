# idvault – Python-Abhängigkeiten
# pip install -r requirements.txt

# Web-Framework
flask>=3.0.0

# Schema-Migrationen. Alembic + SQLAlchemy werden ausschließlich für die
# Versionierung / das Anwenden der Migrationen in alembic/versions/ genutzt –
# die Anwendungs-Queries laufen weiterhin über raw sqlite3.
alembic>=1.13.0
sqlalchemy>=2.0.0

# CSRF-Schutz (VULN-A)
flask-wtf>=1.2.0

# Rate-Limiting am Login (VULN-J)
flask-limiter>=3.5.0

# HTML-Sanitizing für Rich-Text-Felder (VULN-C)
bleach[css]>=6.1.0

# Schutz vor Billion-Laughs / Quadratic-Blowup-DoS beim XML-Parsing in
# scanner/network_scanner.py (#399). Drop-in-Replacement für
# xml.etree.ElementTree.
defusedxml>=0.7.1

# Excel-Export
openpyxl>=3.1.0

# LDAP-Authentifizierung (Active Directory / LDAPS)
ldap3>=2.9.1

# Verschlüsselung für LDAP-Service-Account-Passwort
cryptography>=42.0.0

# Produktiver WSGI-Server (pure Python, threaded, nativer TLS-Support,
# PyInstaller-kompatibel). Wird im EXE-Modus bevorzugt vor dem
# Werkzeug-Dev-Server verwendet.
cheroot>=10.0.0

# Produktion auf Unix (optional, Alternative zu cheroot)
gunicorn>=21.0.0

# Optional: Windows-Dateieigentümer auslesen (nur auf Windows)
pywin32>=306

# Optional: Schnelleres Hashing großer Dateien
xxhash>=3.0.0

# Fuzzy-String-Matching für die Ähnlichkeitsanalyse (Funde ↔ Eigenentwicklungen).
# Optional: Fällt das Paket, nutzt webapp/similarity.py automatisch einen
# Jaccard-Fallback auf reinem Stdlib.
rapidfuzz>=3.6.0

# Teams-Scanner: Microsoft Graph API (optional, nur für teams_scanner.py benötigt)
msal>=1.28.0
requests>=2.31.0
